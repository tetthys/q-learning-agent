services:
  test-runner:
    environment:
      - PYTHONPATH=/app
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    working_dir: /app
    command: ["/bin/bash", "./run/container-test.sh"]
